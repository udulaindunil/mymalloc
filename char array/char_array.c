#include<stdio.h>
#include<string.h>
#include"add.h"

void main(){
	char *x,*y,*z,*p,*t,*b,*k,*r;
	
	x=write(3,'A');
	y=write(7,'B');
	z=write(5,'C');
	//myfree(4);
	p=write(5,'Z');
	t=write(1,'U');
	r=write(15,'i');
	
	display();

}
